version: "3.8"

services:
  claude-web2:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TZ=UTC
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s